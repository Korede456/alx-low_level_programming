#include <stdio.h>

/**
 * main - it prints the alphabet in a lowercase followed
 *	by a new line
 * Return: 0 (success)
 */

int main(void)
{
	char alphabet;

	alphabet = a;
	while (alphabet <= z)
	{
		putchar(alphabet);
		alphabet++;
	};

	putchar("\n");

	return (0);
}
