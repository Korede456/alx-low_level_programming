#include <stdio.h>

/**
 * main - entry point
 * @argc: argument count
 * @argv: string of arguments
 *
 * Retrun: always (0)
 */

int main(int argc, char *argv[])
{
	printf("%d\n", argc);

	return (0);
}
